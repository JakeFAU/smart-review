[settings]
# Black compatibility
profile = black

# Include your known first-party, third-party, and local library directories
known_first_party = src
known_third_party = attrs, openai, google, requests, setuptools, pyGithub

# Ensure import sorting within a certain line length
line_length = 99

# Multi-line output style that matches Black's formatting
multi_line_output = 3

# Use trailing commas like Black
include_trailing_comma = true

# Treat 'src' as first-party to avoid incorrect sorting
src_paths = src

# Ensure imports are sorted within sections (standard library, third-party, first-party)
sections = FUTURE,STDLIB,THIRDPARTY,FIRSTPARTY,LOCALFOLDER

# Ensuring import sorting for common directories
skip = .venv,.git,__pycache__,build,dist,.eggs,node_modules,.tox

# Ignore known errors/warnings
not_skip = __init__.py

# Sorting behavior options
default_section = THIRDPARTY
force_single_line = false
force_sort_within_sections = true
lines_after_imports = 2

# Ensure compatibility with PEP8 naming for better integration
use_parentheses = true
force_grid_wrap = 0
combine_as_imports = true

# Enabling verbose output
verbose = true

# Ignoring unused imports
ignore_whitespace = true
